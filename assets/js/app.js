document.addEventListener('DOMContentLoaded', () => { const mt = document.querySelector('.menu-toggle'); const no = document.querySelector('.nav-overlay'); if(mt){ mt.addEventListener('click', () => { no.classList.toggle('open'); document.body.style.overflow = no.classList.contains('open') ? 'hidden' : 'auto'; }); } });
